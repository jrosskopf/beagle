name: beagle_core
description: Pure-Dart core library for drive-beagle (sync engine, watchers, journal, IPC).
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  yaml: ^3.1.2
  yaml_writer: ^2.0.0
  path: ^1.9.0
  meta: ^1.12.0
  collection: ^1.18.0
  sqlite3: ^2.4.0
  uuid: ^4.4.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
  fake_async: ^1.3.1
