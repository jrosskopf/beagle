/// Stable schema version for every JSON document drive-beagle emits to agents.
///
/// Bump the major component on breaking changes; minor on additive,
/// backwards-compatible additions. CLI consumers should pin on major.
const String beagleSchemaVersion = '1.0';
